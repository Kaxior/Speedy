#include "card.h"

Card createCard(const std::string& value) {
    Card card;
    card.value = value;
    return card;
}

bool isAdjacent(const Card& card1, const Card& card2) {
    // Logique pour vérifier si deux cartes sont adjacentes
    return true;
}
 