#include "player.h"

Player initializePlayer() {
    Player player;
    // Initialiser le stock et les picks
    return player;
}

void drawCard(Player& player) {
    // Ajouter une carte du stock aux picks
}

bool canPlayCard(const Player& player, const Card& card) {
    // Vérifier si une carte est jouable
    return true;
}
 